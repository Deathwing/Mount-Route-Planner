name: Release Addon

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: playground
    env:
      CF_UPLOAD_TOKEN: ${{ secrets.CF_UPLOAD_TOKEN }}
      CF_UPLOAD_TOKEN_NEW: ${{ secrets.CF_UPLOAD_TOKEN_NEW }}
      WAGO_API_TOKEN: ${{ secrets.WAGO_API_TOKEN }}
      WOWI_API_TOKEN: ${{ secrets.WOWI_API_TOKEN }}
      DISCORD_RELEASE_WEBHOOK_URL: ${{ secrets.DISCORD_RELEASE_WEBHOOK_URL }}
      WAGO_PROJECT_ID: nGv7vvNx
      WOWI_ADDON_ID: '27171'
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout tagged addon
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Checkout private release tooling
        uses: actions/checkout@v4
        with:
          repository: Deathwing/Mount-Route-Planner-Internal
          path: playground
          token: ${{ secrets.INTERNAL_REPO_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: playground/package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Verify tag matches addon version
        run: |
          set -euo pipefail
          toc_version=$(sed -n 's/^## Version:[[:space:]]*//p' ../MountRoutePlanner.toc | head -n1)
          test "${toc_version#v}" = "${GITHUB_REF_NAME#v}" || {
            echo "Tag $GITHUB_REF_NAME does not match MountRoutePlanner.toc version $toc_version"
            exit 1
          }

      - name: Generate data and build all packages
        run: bash deployData.sh --skip-upload

      - name: Create checksums and release notes
        run: |
          set -euo pipefail
          (cd dist && sha256sum *.zip > SHA256SUMS)
          awk 'BEGIN { found=0 } /^## / { if (found) exit; found=1 } found' ../CHANGELOG.md > dist/release-notes.md
          test -s dist/release-notes.md || cp ../CHANGELOG.md dist/release-notes.md

      - name: Publish GitHub Release
        run: |
          set -euo pipefail
          if gh release view "$GITHUB_REF_NAME" --repo Deathwing/Mount-Route-Planner >/dev/null 2>&1; then
            gh release edit "$GITHUB_REF_NAME" --repo Deathwing/Mount-Route-Planner --title "$GITHUB_REF_NAME" --notes-file dist/release-notes.md
            gh release upload "$GITHUB_REF_NAME" --repo Deathwing/Mount-Route-Planner --clobber dist/*.zip dist/SHA256SUMS
          else
            gh release create "$GITHUB_REF_NAME" --repo Deathwing/Mount-Route-Planner --title "$GITHUB_REF_NAME" --notes-file dist/release-notes.md dist/*.zip dist/SHA256SUMS
          fi

      - name: Publish split packages to CurseForge
        id: curseforge
        continue-on-error: true
        run: |
          if [[ -z "$CF_UPLOAD_TOKEN" ]]; then echo "CF_UPLOAD_TOKEN not configured; skipping."; exit 0; fi
          node upload.js upload --release

      - name: Publish bundled package to Wago
        id: wago
        continue-on-error: true
        run: |
          if [[ -z "$WAGO_API_TOKEN" ]]; then echo "WAGO_API_TOKEN not configured; skipping."; exit 0; fi
          node publish-wago.js --project-id "$WAGO_PROJECT_ID" --zip dist/MountRoutePlanner_Bundled.zip --toc ../MountRoutePlanner.toc --label "$GITHUB_REF_NAME" --changelog ../CHANGELOG.md

      - name: Publish bundled package to WoWInterface
        id: wowinterface
        continue-on-error: true
        run: |
          if [[ -z "$WOWI_API_TOKEN" ]]; then echo "WOWI_API_TOKEN not configured; skipping."; exit 0; fi
          node publish-wowi.js --addon-id "$WOWI_ADDON_ID" --zip dist/MountRoutePlanner_Bundled.zip --toc ../MountRoutePlanner.toc --version "$GITHUB_REF_NAME" --changelog dist/release-notes.md

      - name: Notify Discord
        id: discord
        continue-on-error: true
        run: |
          if [[ -z "$DISCORD_RELEASE_WEBHOOK_URL" ]]; then echo "DISCORD_RELEASE_WEBHOOK_URL not configured; skipping."; exit 0; fi
          node upload.js discord-release

      - name: Persist Discord release state
        # upload.js records which version+changelog was last announced per package
        # in release-state/discord-release-state.json so unchanged sub-addons (e.g.
        # FarstriderLib) aren't re-announced every release. The playground checkout
        # is fresh each run, so commit the updated state back to the
        # Mount-Route-Planner-Internal repo (checked out here with write access).
        if: steps.discord.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          state_file="release-state/discord-release-state.json"
          if [[ -z "$(git status --porcelain "$state_file")" ]]; then
            echo "Discord release state unchanged; nothing to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$state_file"
          git commit -m "chore: update Discord release state for ${GITHUB_REF_NAME}"
          git push origin HEAD:main

      - name: Verify platform uploads
        if: always()
        env:
          CF_OUTCOME: ${{ steps.curseforge.outcome }}
          WAGO_OUTCOME: ${{ steps.wago.outcome }}
          WOWI_OUTCOME: ${{ steps.wowinterface.outcome }}
          DISCORD_OUTCOME: ${{ steps.discord.outcome }}
        run: |
          [[ "$CF_OUTCOME" != failure && "$WAGO_OUTCOME" != failure && "$WOWI_OUTCOME" != failure && "$DISCORD_OUTCOME" != failure ]] || exit 1